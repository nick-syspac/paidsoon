## ADDED Requirements

### Requirement: Dashboard overview includes a runway resilience summary
The system SHALL include a RunwayGuard summary block in dashboard overview that surfaces runway duration, status, confidence, usable cash, protected cash, and short-term trend.

#### Scenario: Dashboard overview with active runway data
- **WHEN** an entitled user opens dashboard overview
- **THEN** the dashboard shows a runway summary with current status and trend signal
